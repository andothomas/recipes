name: Trigger site build

on:
  push:
    branches-ignore:
      - 'renovate/**'
  delete:

jobs:
  dispatch:
    # `delete` events can't be branch-filtered above, so gate renovate here too
    if: github.event_name == 'push' || !startsWith(github.event.ref, 'renovate/')
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch to recipes-site
        # workflow_dispatch needs only "Actions: read and write" on the token —
        # it can start the build but cannot touch recipes-site's code.
        run: |
          code=$(curl -s -o /dev/null -w '%{http_code}' -X POST \
            -H "Authorization: Bearer ${{ secrets.SITE_DISPATCH_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/andothomas/recipes-site/actions/workflows/deploy.yml/dispatches \
            -d '{"ref":"main"}')
          # A non-204 here usually means SITE_DISPATCH_TOKEN expired — fail loudly.
          [ "$code" = "204" ] || { echo "dispatch failed: HTTP $code"; exit 1; }
